/** Minimum buy (lamports) for First Smart Buy — 0.01 SOL. */
export const FIRST_SMART_BUY_MIN_LAMPORTS = 10_000_000n;

/** Cumulative trading volume (SOL) for Volume Monster — matches missions UI. */
export const VOLUME_MONSTER_MIN_SOL = 1;
