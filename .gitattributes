# Normalize text files to LF in the repo (cross-platform consistency).
* text=auto eol=lf

# Windows scripts keep CRLF.
*.{cmd,bat} text eol=crlf

# Binary assets — no line-ending conversion.
*.{png,jpg,jpeg,gif,webp,ico,svg,pdf,woff,woff2,eot,ttf,otf} binary
