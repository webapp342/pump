---
description: Points agents to pump-tma project skills for research, UI, Next.js, and Web3 work
alwaysApply: true
---

# Pump TMA — Project Skills

Before implementing or researching in this repo, read and follow the matching skill in `.cursor/skills/`:

| Task | Skill |
|------|-------|
| Docs, APIs, versions, current web facts | `research-verified` |
| **Ultra-fast Faz 2–6, DB/Redis/WS architecture** | `.cursor/docs/ultra-fast-architecture-phases-2-6.md` |
| **Strateji + sektör karşılaştırma + tier roadmap** | `.cursor/docs/ultra-fast-strategy-2026.md` |
| **99.9% price accuracy (spot/quote/fill)** | `.cursor/docs/price-accuracy-contract.md` |
| **Seamless Web3 UX / AA araştırma** | `.cursor/docs/seamless-web3-ux-research-2026.md` |
| **Self-hosted bundler (Alto, no Pimlico SaaS)** | `.cursor/docs/self-hosted-bundler-2026.md` |
| **Account Abstraction (Telegram/Kernel/Alto)** | `pump-tma-account-abstraction` |
| **Telegram OIDC login / auth modal / BotFather** | `pump-tma-telegram-auth` |
| **Admin console IA, copy, layout** | `pump-tma-admin-console` |
| Design system, tokens, nav, layout specs | `pump-tma-design-system` |
| Pages, modals, forms, styling, trading UI | `pump-tma-ui-ux` |
| App Router pages, API routes, PostgreSQL layer | `pump-tma-nextjs` |
| wagmi, viem, AppKit, on-chain writes | `pump-tma-web3` |
| Smart wallet, session keys, gasless tx | `pump-tma-account-abstraction` |

## Defaults

- **UI work:** Read `pump-tma-design-system` first, then `pump-tma-ui-ux`. Top nav + mobile bottom bar — no left sidebar.
- Verify library APIs via Context7 (`user-context7`) — do not guess versions or syntax.
- Match existing patterns in `src/` before introducing new libraries or UI primitives.
- No shadcn/MUI; use `globals.css` component classes and `pump-*` tokens.
- Dev server: `npm run dev` on port **3012**.
